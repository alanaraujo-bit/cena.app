# React Native / Expo + Metro do not resolve pnpm's default symlinked store well.
# A hoisted node_modules keeps the mobile bundler happy inside the monorepo.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
